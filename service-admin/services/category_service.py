"""★ 小D：分类管理业务逻辑层。

待填充内容：
  - create_category(name, parent_id) → 创建 + 清除分类树缓存
  - update_category(id, name, parent_id) → 更新 + 清除缓存
  - delete_category(id) → 检查商品引用 → 删除 + 清除缓存
  - get_category_tree() → 读取 + 构造成树结构 + 缓存
"""
