# 🛒 E-Shop 第二轮开发分工文档

> **文档版本：** v1.0
> **编制日期：** 2026-06-16
> **开发模式：** 4 人并行开发 + 最后合并集成

---

## 一、团队成员与分支分配

| 人员 | 分支 | 负责模块 | 核心产出 |
|------|------|----------|----------|
| 🅰️ 小A | `feat/xiaoa-ai-frontend` | AI 客服 + 前端全功能 | AI 新增工具、RAG 知识库、前端评价UI、支付UI、聊天增强 |
| 🅱️ 小B | `feat/xiaob-reviews` | 商品评价系统 | reviews 数据库 + 后端 API + 内部接口 + 商品评分 |
| ©️ 小C | `feat/xiaoc-payment-order` | 模拟支付 + 对话下单 | 支付状态机 + 多种支付方式 + 直接创建订单内部接口 |
| 🅳 小D | `feat/xiaod-admin-infra` | 管理后台 + 基础设施 | 评价管理、Nginx 路由、FAQ 管理界面、集成测试 |

---

## 二、功能模块与依赖关系

### 2.1 模块依赖图

```
                           ┌──────────────────────────┐
                           │  init.sql 数据库变更       │
                           │  (小B 负责)                │
                           └────────┬─────────────────┘
                                    │
              ┌─────────────────────┼─────────────────────┐
              │                     │                     │
              ▼                     ▼                     ▼
   ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
   │ 小B: 评价后端 API │  │ 小C: 支付/下单    │  │ 小D: 基础设施    │
   │ POST/GET reviews │  │ 支付状态机        │  │ Nginx路由        │
   │ 评价统计          │  │ POST /internal/   │  │ docker-compose   │
   │ 内部接口          │  │   orders/create   │  │ 集成测试         │
   └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
            │                     │                     │
            └──────────┬──────────┘                     │
                       │                                │
                       ▼                                │
           ┌──────────────────────┐                     │
           │ 小A: AI 服务集成     │                     │
           │ create_order tool    │◄────────────────────┘
           │ pay_order tool       │
           │ get_product_reviews  │
           │ RAG FAQ 检索         │
           │ 对话持久化           │
           └──────────┬───────────┘
                      │
                      ▼
           ┌──────────────────────┐
           │ 小A: 前端全功能      │
           │ 评价列表/表单        │
           │ 支付方式选择弹窗     │
           │ ChatPopup 增强       │
           │ 订单评价入口         │
           └──────────────────────┘
```

### 2.2 依赖关系表

| 任务 | 前置依赖 | 可并行？ |
|------|----------|----------|
| 小B: 评价后端 API | init.sql（自己负责） | ✅ 独立 |
| 小C: 支付/下单 | 无 | ✅ 独立 |
| 小D: Nginx + docker-compose | 无 | ✅ 独立 |
| 小D: 集成测试 | 所有模块完成 | ❌ 需等待 |
| 小A: AI 工具 create_order | 小C: 内部端点完成 | ⚠️ 接口定义后可先行 |
| 小A: AI 工具 get_product_reviews | 小B: 内部接口完成 | ⚠️ 接口定义后可先行 |
| 小A: RAG FAQ | 无 | ✅ 独立 |
| 小A: 前端评价UI | 小B: API 完成 | ⚠️ 接口定义后可先行 |
| 小A: 前端支付UI | 小C: API 完成 | ⚠️ 接口定义后可先行 |

---

## 三、详细任务分配

### 🅱️ 小B — 商品评价系统（user-product）

> 分支：`feat/xiaob-reviews`

#### 3.1 数据库变更 (`docker/init.sql`)

在 `shop.after_sale_requests` 表定义之后、`customer_service` schema 之前插入：

```sql
-- ============================================
-- 商品评价表（v2.0）
-- ============================================
CREATE TABLE IF NOT EXISTS shop.reviews (
    id          SERIAL PRIMARY KEY,
    product_id  INTEGER NOT NULL REFERENCES shop.products(id),
    user_id     INTEGER NOT NULL REFERENCES shop.users(id),
    order_id    INTEGER NOT NULL REFERENCES shop.orders(id),
    rating      SMALLINT NOT NULL CHECK(rating >= 1 AND rating <= 5),
    content     TEXT DEFAULT '',
    status      VARCHAR(20) DEFAULT 'visible',
    created_at  TIMESTAMPTZ DEFAULT NOW(),
    updated_at  TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id, order_id, product_id)
);

CREATE INDEX IF NOT EXISTS idx_reviews_product ON shop.reviews(product_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_reviews_user ON shop.reviews(user_id);

-- 商品表新增评分缓存字段
ALTER TABLE shop.products ADD COLUMN IF NOT EXISTS avg_rating DECIMAL(3,2) DEFAULT 0;
ALTER TABLE shop.products ADD COLUMN IF NOT EXISTS review_count INTEGER DEFAULT 0;
```

#### 3.2 新建 `services/review_service.py`

功能函数：

```python
def create_review(user_id: int, product_id: int, order_id: int, rating: int, content: str) -> dict
    """创建评价（校验：订单归属当前用户、订单已支付、未评价过）。"""
    # 1. 验证 order 属于 user 且 status = 'paid'
    # 2. 验证 UNIQUE(user_id, order_id, product_id) — 不重复评价
    # 3. INSERT INTO shop.reviews
    # 4. UPDATE shop.products SET avg_rating = (子查询), review_count = (子查询)
    # 5. 返回评价记录

def get_product_reviews(product_id: int, page: int, size: int) -> tuple
    """获取商品评价列表（分页，按时间倒序，JOIN users 获取 nickname）。"""

def get_review_stats(product_id: int) -> dict
    """获取评价统计：avg_rating, total_count, distribution(各星级数量)。"""

def get_user_reviews(user_id: int) -> list
    """获取当前用户的评价列表。"""
```

#### 3.3 新建 `routers/review_router.py`

| 端点 | 方法 | 说明 | 鉴权 |
|------|------|------|------|
| `/c-endpoint/reviews` | POST | 创建评价 | JWT（需已登录） |
| `/c-endpoint/reviews/product/{product_id}` | GET | 商品评价列表 | 公开 |
| `/c-endpoint/reviews/product/{product_id}/stats` | GET | 评价统计 | 公开 |
| `/c-endpoint/reviews/user/me` | GET | 自己的评价 | JWT |

#### 3.4 修改 `routers/internal_router.py`

新增内部端点（供 ai-service 调用）：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/internal/reviews/product/{product_id}` | GET | 获取商品最新评价（用于 AI 总结，取最近 20 条） |
| `/internal/reviews/product/{product_id}/stats` | GET | 获取评价统计（avg_rating, distribution） |

#### 3.5 修改 `services/product_service.py`

- `get_product_by_id()` 返回结果中附加 `avg_rating` 和 `review_count`

#### 3.6 修改 `main.py`

- 注册 `review_router`

---

### ©️ 小C — 模拟支付 + 对话下单支撑（order-trade）

> 分支：`feat/xiaoc-payment-order`

#### 4.1 修改 `services/order_service.py`

**拆分支付流程（核心改动）：**

| 旧函数 | 新函数 | 说明 |
|--------|--------|------|
| `pay_order()` | ❌ 删除 | 拆分为以下三个 |
| — | `initiate_payment(order_id, user_id, payment_method)` | ✅ 新建：加锁校验 → 创建 payment_record(status=processing) + 交易流水号 → 返回 |
| — | `complete_payment(payment_id, success, error_message)` | ✅ 新建：加锁 → 更新状态为 success/failed → 更新 order.status → 创建物流(成功时) |
| — | `get_payment_status(order_id, user_id)` | ✅ 新建：查询最新支付记录状态 |

支付配置常量（写在文件顶部）：
```python
PAYMENT_METHODS = {"wechat", "alipay", "card", "balance", "mock"}
```

**新增直接下单函数：**

```python
def create_order_direct(user_id: int, items: list, address: str) -> dict
    """AI 对话下单用：items = [{"product_id": 1, "quantity": 2}, ...]"""
    # 事务：锁定商品 → 校验库存 → 扣减 → 创建订单+明细 → 返回
```

#### 4.2 修改 `routers/order_router.py`

| 改动项 | 说明 |
|--------|------|
| `POST /orders/{id}/pay` | 接受 `payment_method` 参数（wechat/alipay/card/balance/mock），调用 `initiate_payment` |
| `GET /orders/{id}/payment` | ✅ 新增：调用 `get_payment_status`，供前端轮询支付结果 |

新增 Pydantic 模型：
```python
class PayOrderRequest(BaseModel):
    payment_method: str = "mock"
```

#### 4.3 修改 `routers/internal_router.py`

| 端点 | 方法 | 说明 |
|------|------|------|
| `/internal/orders/create` | POST | ✅ 新增：接受 `{user_id, items, address}`，调用 `create_order_direct()` |

#### 4.4 修改 `services/scheduler_jobs.py`

新增 `process_payments()` 函数（每 3 秒执行一次）：

```python
def process_payments():
    """扫描 payment_records WHERE status='processing'，
       根据 created_at 判断是否超过模拟延迟（1-3 秒随机），
       95% 标记为 success，5% 标记为 failed。
       成功时：更新 order.status='paid' + 创建物流记录。"""
```

支付配置常量：
```python
PAYMENT_DELAY_MIN_SECONDS = 1
PAYMENT_DELAY_MAX_SECONDS = 3
PAYMENT_FAILURE_RATE = 0.05
```

#### 4.5 修改 `main.py`

在 `_register_scheduler_jobs()` 中注册 `process_payments` 定时任务（interval=3秒）。

---

### 🅳 小D — 管理后台 + 基础设施 + 测试

> 分支：`feat/xiaod-admin-infra`

#### 5.1 管理后台 — 评价管理 (`service-admin/`)

**新增评价管理页面/接口：**

| 端点 | 方法 | 说明 | 鉴权 |
|------|------|------|------|
| `/b-endpoint/reviews` | GET | 评价列表（分页，支持按商品/星级筛选） | admin |
| `/b-endpoint/reviews/{id}/status` | PATCH | 隐藏/显示评价（visible ↔ hidden） | admin |
| `/b-endpoint/reviews/product/{product_id}` | GET | 某商品的评价管理 | admin |

建议文件：
- 新建 `routers/review_router.py` — 管理后台评价路由
- 新建 `services/review_service.py` — 管理后台评价业务（可调用 user-product 内部接口或直接查 DB）
- 修改 `main.py` — 注册 review_router

#### 5.2 FAQ 管理界面 (`service-admin/`)

**新增 FAQ 管理功能（管理后台中可以维护知识库）：**

| 端点 | 方法 | 说明 | 鉴权 |
|------|------|------|------|
| `/b-endpoint/faq` | GET | FAQ 列表 | admin |
| `/b-endpoint/faq` | POST | 添加 FAQ（question + answer，embedding 由 ai-service 生成） | admin |
| `/b-endpoint/faq/{id}` | PUT | 修改 FAQ | admin |
| `/b-endpoint/faq/{id}` | DELETE | 删除 FAQ | admin |

建议文件：
- 新建 `routers/faq_router.py`
- 新建 `services/faq_admin_service.py`

#### 5.3 Nginx 路由 — `docker/nginx/nginx.conf`

在 user-product 的 location 组中新增 reviews 路由：

```nginx
location ~ ^/api/shop/c-endpoint/reviews/(.*)$ {
    set $upstream_path /c-endpoint/reviews/$1$is_args$args;
    proxy_pass http://user-product:8001$upstream_path;
}
```

同时在 admin 的 location 组中新增 reviews 和 faq 管理路由。

#### 5.4 docker-compose 环境变量 — `docker/docker-compose.yml`

ai-service 配置段新增数据库连接：

```yaml
ai-service:
  environment:
    - DATABASE_URL=${DATABASE_URL:-postgresql://user:1234@postgres:5432/agent}
```

#### 5.5 集成测试 — `tests/integration_test.py`

在现有 63 项测试基础上新增第二轮测试用例：

| 测试模块 | 项数 | 覆盖内容 |
|----------|:----:|----------|
| 评价创建 | 3 | 正常创建、重复评价拒绝、非已购用户拒绝 |
| 评价列表 | 2 | 商品评价列表、评价统计 |
| 评价权限 | 1 | 管理后台隐藏评价 |
| 支付 | 4 | 各支付方式发起、支付状态查询、支付成功、支付失败 |
| 对话下单 | 3 | 直接创建订单、库存不足、内部权限校验 |
| FAQ 管理 | 2 | 添加问答、列表查询 |

---

### 🅰️ 小A — AI 客服增强 + 前端全功能

> 包含两个分支：
> - `feat/xiaoa-ai-service`（AI 服务改动）
> - `feat/xiaoa-frontend`（前端改动）

#### 6.1 AI 服务 — 新增工具 (`ai-service/services/tools.py`)

在 `TOOL_DEFINITIONS` 中新增 3 个工具：

```python
# 7. create_order — 对话下单
{
    "type": "function",
    "function": {
        "name": "create_order",
        "description": "为用户创建订单（多轮对话确认商品、数量、地址后调用）",
        "parameters": {
            "type": "object",
            "properties": {
                "items": {
                    "type": "array",
                    "items": {
                        "type": "object",
                        "properties": {
                            "product_id": {"type": "integer"},
                            "quantity": {"type": "integer"}
                        },
                        "required": ["product_id", "quantity"]
                    }
                },
                "address": {"type": "string"}
            },
            "required": ["items", "address"]
        }
    }
}

# 8. pay_order — 执行支付
{
    "type": "function",
    "function": {
        "name": "pay_order",
        "description": "执行订单支付",
        "parameters": {
            "type": "object",
            "properties": {
                "order_id": {"type": "integer"},
                "payment_method": {
                    "type": "string",
                    "enum": ["wechat", "alipay", "card", "balance"]
                }
            },
            "required": ["order_id"]
        }
    }
}

# 9. get_product_reviews — 获取商品评价
{
    "type": "function",
    "function": {
        "name": "get_product_reviews",
        "description": "获取某商品的用户评价和评分统计",
        "parameters": {
            "type": "object",
            "properties": {
                "product_id": {"type": "integer"},
                "product_name": {"type": "string", "description": "商品名称（便于AI引用）"}
            },
            "required": ["product_id"]
        }
    }
}
```

在 `execute_tool()` 中注册新工具的分发逻辑。

#### 6.2 AI 服务 — ShopClient 新增方法 (`ai-service/services/shop_client.py`)

```python
def create_order(self, user_id: int, items: list, address: str) -> dict
    """调用 order-trade:8002/internal/orders/create"""

def pay_order(self, order_id: int, user_id: int, payment_method: str) -> dict
    """调用 order-trade:8002/c-endpoint/orders/{id}/pay"""

def get_product_reviews(self, product_id: int) -> dict
    """调用 user-product:8001/internal/reviews/product/{product_id}"""

def get_product_review_stats(self, product_id: int) -> dict
    """调用 user-product:8001/internal/reviews/product/{product_id}/stats"""
```

#### 6.3 AI 服务 — RAG FAQ 检索 (`ai-service/services/faq_service.py`)

完整实现以下函数：

```python
def generate_embedding(text: str) -> list[float]
    """调用 LLM API 生成 1024 维向量（使用 DeepSeek embeddings API）。"""

def search_faq(query: str, threshold: float = 0.85) -> str | None
    """生成 query embedding → pgvector 余弦相似度查询
       → 返回最匹配的 answer（相似度 ≥ threshold）"""

def add_faq(question: str, answer: str, category: str = "general") -> int
    """新增 FAQ + 生成 embedding 写入 faq_embeddings 表"""
```

**FAQ 种子数据**（约 20-30 条，写在独立脚本 `scripts/seed_faq.py`）：

| 类别 | 数量 | 示例 |
|------|:----:|------|
| 退货政策 | 4 | "退货需要什么条件？" → "商品不影响二次销售，签收后 7 天内可申请退货退款" |
| 物流时效 | 4 | "下单后多久发货？" → "通常在支付成功后 24 小时内发货" |
| 支付方式 | 3 | "支持哪些支付方式？" → "支持微信支付、支付宝、银行卡、余额支付" |
| 账户安全 | 3 | "如何修改密码？" → "在个人中心 → 安全设置中可修改登录密码" |
| 商品知识 | 4 | "商品材质如何查看？" → "在商品详情页的描述中可以查看材质信息" |
| 售后 | 3 | "如何申请售后？" → "在订单详情页点击「申请售后」即可提交请求" |
| 优惠活动 | 2 | "有优惠券吗？" → "关注公众号可领取新用户优惠券" |

#### 6.4 AI 服务 — 对话持久化 (`ai-service/services/conversation_service.py`)

重构为数据库持久化（原为内存 dict）：

```python
def create_conversation(user_id: str) -> str
    """INSERT INTO customer_service.conversations → 返回 UUID"""

def add_message(conv_id: str, role: str, content: str, turn_number: int, metadata: dict = None)
    """INSERT INTO customer_service.messages"""

def get_history(conv_id: str) -> list
    """SELECT FROM customer_service.messages WHERE conversation_id=? ORDER BY turn_number"""

def update_conversation_title(conv_id: str, title: str)
    """更新对话标题"""
```

#### 6.5 AI 服务 — 数据库连接 (`ai-service/services/database.py`)

```python
"""ai-service 的 PostgreSQL 连接池（连接 customer_service schema）。"""
# 复用 shop_shared 的 get_connection/get_cursor 模式
# 或从零搭建简单的 psycopg2 连接池
```

#### 6.6 AI 服务 — 更新 SYSTEM_PROMPT (`ai-service/services/llm_service.py`)

在 SYSTEM_PROMPT 中新增：
- AI 具备下单能力及约束规则（必须用户确认后才能下单）
- AI 具备支付能力（支持微信/支付宝/银行卡/余额）
- AI 可查询商品评价并总结分析
- AI 可回答 FAQ 知识库中的平台政策问题

#### 6.7 AI 服务 — 集成 FAQ 检索 (`ai-service/routers/chat_router.py`)

在 `_resolve_tool_calls()` 之前插入 FAQ 检索步骤：

```
用户提问 → FAQ 检索
  ├─ 匹配度 ≥ 0.85 → 直接返回 FAQ 答案（跳过 LLM）
  └─ 匹配度 < 0.85 → 进入 LLM + 工具调用流程
```

#### 6.8 AI 服务 — 环境配置

- `requirements.txt` 新增：`psycopg2-binary`
- `docker-compose.yml` 新增环境变量（由小D在 5.4 节处理）

#### 6.9 前端 — 商品评价相关

| 文件 | 改动 |
|------|------|
| `api/review.ts` | ✅ 新建：`createReview()`, `getProductReviews()`, `getProductReviewStats()`, `getUserReviews()` |
| `types/index.ts` | 新增 `Review`, `ReviewStats` 类型定义 |
| `components/StarRating.tsx` | ✅ 新建：星级评分组件（支持展示模式 + 交互模式） |
| `components/ReviewForm.tsx` | ✅ 新建：评价表单（星级选择器 + 文本框 + 提交） |
| `pages/product/ProductDetailPage.tsx` | 商品信息下方新增「商品评价」区域：评价统计概览 + 评价列表（分页） |
| `pages/order/OrderDetailPage.tsx` | 已支付且已签收的订单中，每个商品旁显示「去评价」按钮 → 弹窗 ReviewForm |

#### 6.10 前端 — 支付增强

| 文件 | 改动 |
|------|------|
| `api/order.ts` | `payOrder()` 新增 `payment_method` 参数 |
| `types/index.ts` | 新增 `PaymentMethod`、`PaymentRecord` 类型 |
| `pages/order/OrderDetailPage.tsx` | 支付按钮改为弹窗选择支付方式；支付中展示处理动画（轮询 `/orders/{id}/payment`）；失败展示重试按钮 |

#### 6.11 前端 — ChatPopup 增强

| 文件 | 改动 |
|------|------|
| `ChatPopup.tsx` | AI 返回中检测订单信息 → 渲染为可点击订单卡片（跳转订单详情）；AI 询问支付时 → 展示支付方式选择快捷按钮；AI 评价总结时 → 卡片展示评分星级和统计摘要 |
| `ChatPopup.css` | 订单卡片、支付按钮、评价卡片的样式 |

---

## 四、接口契约定义

### 4.1 跨服务接口

#### 小B ↔ 小A（商品评价内部接口）

```
GET /internal/reviews/product/{product_id}  →  ai-service 调用
Response: {
  "code": 0,
  "data": {
    "reviews": [{"id", "user_id", "nickname", "rating", "content", "created_at"}, ...],
    "stats": {"avg_rating": 4.2, "total_count": 125, "distribution": {1: 2, 2: 5, 3: 15, 4: 45, 5: 58}}
  }
}
```

#### 小C ↔ 小A（直接下单内部接口）

```
POST /internal/orders/create  →  ai-service 调用
Request: {
  "user_id": 1,
  "items": [{"product_id": 1, "quantity": 2}],
  "address": "深圳市南山区科技园"
}
Response: {"code": 0, "data": {"id": 1024, "total_amount": 2598.00, "status": "pending"}}
```

#### 小C ↔ 小A（支付接口）

```
POST /c-endpoint/orders/{id}/pay  →  ai-service 调用
Request: {"payment_method": "wechat"}
Response: {"code": 0, "data": {"payment_id": 1, "status": "processing", "transaction_no": "TXN20260616..."}}

GET /c-endpoint/orders/{id}/payment  →  前端轮询
Response: {"code": 0, "data": {"status": "success", "method": "wechat", "finished_at": "..."}}
```

### 4.2 前端 ↔ 后端接口

#### 评价 API

```
POST /api/shop/c-endpoint/reviews
Request: {"product_id": 1, "order_id": 1024, "rating": 5, "content": "很好用！"}
Response: {"code": 0, "data": {"id": 1, "rating": 5, ...}}

GET /api/shop/c-endpoint/reviews/product/{product_id}?page=1&size=10
Response: {"code": 0, "data": {"items": [...], "total": 125, "page": 1, "size": 10}}

GET /api/shop/c-endpoint/reviews/product/{product_id}/stats
Response: {"code": 0, "data": {"avg_rating": 4.2, "total_count": 125, "distribution": {1:2, 2:5, ...}}}

GET /api/shop/c-endpoint/reviews/user/me
Response: {"code": 0, "data": [{"id": 1, "product_id": 1, "rating": 5, ...}]}
```

#### 管理后台评价 API

```
GET /api/shop/b-endpoint/reviews?page=1&size=20
Response: {"code": 0, "data": {"items": [...], "total": 125}}

PATCH /api/shop/b-endpoint/reviews/{id}/status
Request: {"status": "hidden"}
Response: {"code": 0, "data": {"id": 1, "status": "hidden"}}
```

#### 管理后台 FAQ API

```
GET /api/shop/b-endpoint/faq
POST /api/shop/b-endpoint/faq
Request: {"question": "...", "answer": "...", "category": "退货政策"}
PUT /api/shop/b-endpoint/faq/{id}
DELETE /api/shop/b-endpoint/faq/{id}
```

---

## 五、开发顺序建议

### Phase 1 — 基础设施（第 1-2 天）

| 顺序 | 任务 | 负责人 | 说明 |
|:----:|------|--------|------|
| 1.1 | init.sql 新增 reviews 表 + products 扩展字段 | 小B | 数据库 schema 先行 |
| 1.2 | Nginx 新增 reviews 路由 | 小D | 基础设施先行 |
| 1.3 | docker-compose 新增 ai-service 数据库URL | 小D | 基础设施先行 |
| 1.4 | ai-service 新增 psycopg2-binary 依赖 | 小A | 依赖先行 |

### Phase 2 — 后端核心逻辑（第 2-4 天，可并行）

| 顺序 | 任务 | 负责人 | 说明 |
|:----:|------|--------|------|
| 2.1 | 评价后端 API（review_service + review_router） | 小B | ✅ 独立开发 |
| 2.2 | 支付状态机 + 直接下单内部接口 | 小C | ✅ 独立开发 |
| 2.3 | 管理后台评价 + FAQ 管理 | 小D | ✅ 独立开发 |
| 2.4 | AI 工具定义 + ShopClient + 数据库连接池 | 小A | ⚠️ 部分依赖 2.2 |

### Phase 3 — AI + 前端（第 3-6 天）

| 顺序 | 任务 | 负责人 | 说明 |
|:----:|------|--------|------|
| 3.1 | RAG FAQ 检索 + 对话持久化 | 小A | ✅ 独立开发 |
| 3.2 | 前端评价组件（StarRating, ReviewForm, 评价列表） | 小A | ⚠️ 后端 API 就绪后联调 |
| 3.3 | 前端支付 UI（方式选择 + 动画 + 轮询） | 小A | ⚠️ 后端 API 就绪后联调 |
| 3.4 | ChatPopup 增强（订单卡片 + 支付卡片） | 小A | ⚠️ 后端 API 就绪后联调 |
| 3.5 | SYSTEM_PROMPT 更新 | 小A | ✅ 独立 |

### Phase 4 — 集成测试（第 6-7 天）

| 顺序 | 任务 | 负责人 | 说明 |
|:----:|------|--------|------|
| 4.1 | 新增集成测试用例 | 小D | 各模块完成后 |
| 4.2 | 全链路调试 | 全员 | 按第一轮合并流程 |

---

## 六、首次开发经验 & 本轮避坑指南

参考第一轮 `integration.md` 中的 7 个合并后修复的问题，本轮需特别注意：

### 6.1 第一轮教训

| 问题 | 避免措施 |
|------|----------|
| baseURL 被改 | ✅ **本轮不动** `frontend/src/api/client.ts` 的 baseURL，保持 `/api/shop` |
| Nginx 路由缺失 | ✅ 小D 提前加上 reviews 路由（Phase 1） |
| Vite 代理端口错误 | ✅ 如新增前端代理，检查端口号是否为 8004（ai-service） |
| 内部接口双重前缀 | ✅ 保持 `internal_router.py` 不设 prefix，只在 main.py 设一次 |
| TypeScript 类型不一致 | ✅ 小A 先定义好 `types/index.ts` 的新类型，其他前端代码依赖之 |
| 支付返回数据不全 | ✅ 小C 提供 `GET /orders/{id}/payment` 独立查询接口 |

### 6.2 本轮新增注意事项

- **评价 UNIQUE 约束**：`UNIQUE(user_id, order_id, product_id)` 确保一人一订单一商品只能评一次，后端要在 SQL 层面做约束 + 应用层友好提示
- **支付状态机**：`initiate_payment` 和 `complete_payment` 是异步两阶段，确保加锁顺序一致避免死锁
- **FAQ 向量维度**：`faq_embeddings.embedding` 是 `vector(1024)`，embedding 模型输出必须是 1024 维
- **ai-service 新增 DB 依赖**：原 ai-service 不直接连数据库，新增 `database.py` 时注意连接池管理、环境变量配置
- **对话持久化兼容**：旧内存对话在服务重启后丢失，这是预期行为，不需要做数据迁移

---

## 七、文件改动清单

### 7.1 后端改动

| 服务 | 文件 | 操作 | 负责人 |
|------|------|------|--------|
| **全局** | `docker/init.sql` | 修改 | 小B |
| | `docker/nginx/nginx.conf` | 修改 | 小D |
| | `docker/docker-compose.yml` | 修改 | 小D |
| | `tests/integration_test.py` | 修改 | 小D |
| **user-product** | `services/review_service.py` | ✅ 新建 | 小B |
| | `routers/review_router.py` | ✅ 新建 | 小B |
| | `routers/internal_router.py` | 修改 | 小B |
| | `services/product_service.py` | 修改 | 小B |
| | `main.py` | 修改 | 小B |
| **order-trade** | `services/order_service.py` | 修改 | 小C |
| | `routers/order_router.py` | 修改 | 小C |
| | `routers/internal_router.py` | 修改 | 小C |
| | `services/scheduler_jobs.py` | 修改 | 小C |
| | `main.py` | 修改 | 小C |
| **admin** | `services/review_service.py` | ✅ 新建 | 小D |
| | `routers/review_router.py` | ✅ 新建 | 小D |
| | `services/faq_admin_service.py` | ✅ 新建 | 小D |
| | `routers/faq_router.py` | ✅ 新建 | 小D |
| | `main.py` | 修改 | 小D |
| **ai-service** | `services/tools.py` | 修改 | 小A |
| | `services/shop_client.py` | 修改 | 小A |
| | `services/faq_service.py` | 重写 | 小A |
| | `services/conversation_service.py` | 重写 | 小A |
| | `services/database.py` | ✅ 新建 | 小A |
| | `services/llm_service.py` | 修改 | 小A |
| | `routers/chat_router.py` | 修改 | 小A |
| | `requirements.txt` | 修改 | 小A |

### 7.2 前端改动

| 文件 | 操作 | 负责人 |
|------|------|--------|
| `api/review.ts` | ✅ 新建 | 小A |
| `api/order.ts` | 修改 | 小A |
| `types/index.ts` | 修改 | 小A |
| `components/StarRating.tsx` | ✅ 新建 | 小A |
| `components/ReviewForm.tsx` | ✅ 新建 | 小A |
| `components/ChatPopup.tsx` | 修改 | 小A |
| `components/ChatPopup.css` | 修改 | 小A |
| `pages/product/ProductDetailPage.tsx` | 修改 | 小A |
| `pages/order/OrderDetailPage.tsx` | 修改 | 小A |

---

## 八、合并策略

参照第一轮流程：

```bash
# 1. 确保 master 已含小B的基础代码
git checkout master
git pull origin master

# 2. 按顺序合并（串行避免冲突累积）
git merge origin/feat/xiaob-reviews         # 小B: 评价系统
git merge origin/feat/xiaoc-payment-order   # 小C: 支付+下单
git merge origin/feat/xiaod-admin-infra     # 小D: 管理后台+基础设施

# 3. 恢复小A的工作（如果小A是本地开发）
git stash pop  # 如有 stash

# 4. 运行测试
python tests/integration_test.py
```

冲突处理原则：
- `service-user-product/*` → 保留 master（小B）
- `service-order-trade/*` → 保留 master（小C）
- `service-admin/*` → 保留 master（小D）
- `ai-service/*` → 保留 master（小A）
- `frontend/*` → 保留 master（小A）
- `docker/*` → 保留 master（小D）
- `tests/*` → 保留 master（小D）

---

## 九、验收标准

### 全链路测试场景

| 场景 | 操作 | 预期结果 |
|------|------|----------|
| 对话下单 | ChatPopup 输入"帮我下单买1个智能手表Pro，送到深圳市南山区" | AI 确认后创建订单，数据库可见 |
| 选择支付方式 | 在订单详情页点击去支付，选择微信支付 | 显示处理中动画，3 秒后状态变为已支付 |
| AI 支付引导 | 下单后对 AI 说"现在支付" | AI 调用 pay_order，支付成功 |
| 查看评价 | 在商品详情页下拉到评价区域 | 展示评价列表和评分统计 |
| 提交评价 | 在已签收订单中点击去评价 | 提交成功，评价显示在商品详情页 |
| AI 总结评价 | 对 AI 说"智能手表 Pro 评价怎么样" | AI 调取评价数据并总结优缺点 |
| 平台政策问答 | 对 AI 说"退货政策是什么" | 返回 FAQ 预置答案（匹配度≥0.85） |
| FAQ 管理 | 管理员登录 → FAQ 管理页面 → 新增问答 | FAQ 添加成功，AI 可检索到 |

### 测试通过率目标

```
第1轮: 63/63 → 100% ✅
第2轮: 63 + 新增 ≥ 15 项 → 78/78 → 100% ✅
```
