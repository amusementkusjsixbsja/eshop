# 🛒 E-Shop 第二轮开发需求文档

> **项目名称：** 电商平台 (E-Shop)
> **版 本：** v2.0（第二轮开发）
> **编制日期：** 2026-06-16
> **文档状态：** 初稿 ✓ (已确认)

---

## 1. 概述

### 1.1 本轮目标

在第一轮基础功能（用户注册登录、商品浏览、购物车、下单支付、物流模拟、售后管理、AI 客服对话）之上，进行以下四个方向的增强：

| 序号 | 模块 | 核心能力 | 对应服务 |
|:----:|------|----------|----------|
| 1 | 🗣️ AI 对话下单 | 用户通过自然语言对话完成完整下单流程 | ai-service + order-trade |
| 2 | 💳 模拟支付 | 支付状态机 + 多种支付方式 + 可配置延迟 | order-trade |
| 3 | 📚 RAG 客服知识库 | 基于向量检索的 FAQ 知识库 + 对话持久化 | ai-service |
| 4 | ⭐ 商品评价 | 用户评价系统 + AI 总结分析商品口碑 | user-product + ai-service + frontend |

### 1.2 当前项目架构回顾

```
用户 → Nginx (:80)
  ├── /api/shop/c-endpoint/auth/*      → user-product:8001  # 认证
  ├── /api/shop/c-endpoint/products/*  → user-product:8001  # 商品/分类
  ├── /api/shop/c-endpoint/addresses/* → user-product:8001  # 地址
  ├── /api/shop/c-endpoint/cart/*      → order-trade:8002   # 购物车
  ├── /api/shop/c-endpoint/orders/*    → order-trade:8002   # 订单
  ├── /api/shop/c-endpoint/logistics/* → order-trade:8002   # 物流
  ├── /api/shop/c-endpoint/after-sales/* → order-trade:8002 # 售后
  ├── /api/shop/b-endpoint/*           → admin:8003         # 管理后台
  ├── /api/ai/*                        → ai-service:8004    # AI 客服
  └── /*                               → frontend:5173      # 前端页面
```

---

## 2. 功能需求

### 2.1 🗣️ AI 对话下单

#### 2.1.1 需求描述

用户通过 AI 客服对话窗口，以自然语言表达购买意图 → AI 多轮确认商品、数量、地址等信息 → AI 直接创建订单 → 引导用户支付。

**参考：** 阿里千问「喝奶茶」对话下单体验

#### 2.1.2 交互流程

```
用户: "帮我下单买2个智能手表"
AI:   "好的，为你找到以下智能手表：
      1. 智能手表 Pro（¥1299）— 库存充足
      2. 智能手表 Lite（¥799）— 库存充足
      请问你想要哪一款？"
用户: "Pro 吧"
AI:   "好的，智能手表 Pro × 2，共计 ¥2598。
      你的默认收货地址是「北京市朝阳区xxx」，
      是否使用此地址？"
用户: "是的"
AI:   "✅ 订单已创建！订单号 #1024，金额 ¥2598，状态：待支付。
      是否现在进行支付？"
```

#### 2.1.3 功能要点

| 功能 | 说明 |
|------|------|
| 商品搜索与推荐 | AI 根据用户描述搜索匹配商品，展示简要列表供选择 |
| 多轮确认 | AI 主动确认商品、数量、地址、总价 |
| 一键下单 | 调用后端 API 直接创建订单（跳过购物车环节） |
| 下单后引导 | 创建成功后提示订单信息，询问是否支付 |
| 地址选择 | 使用用户默认地址，或让用户提供新地址 |

#### 2.1.4 后端改动

##### ai-service

| 文件 | 改动内容 |
|------|----------|
| `services/tools.py` | 新增 AI Tool 定义：`create_order`（参数：商品 ID、数量、地址文本等） |
| `clients/shop_client.py` | 新增方法 `create_order()`，调用 `order-trade` 内部接口创建订单；新增 `get_addresses()`，获取用户地址列表 |
| `services/llm_service.py` | 更新 `SYSTEM_PROMPT`，新增下单能力说明和规则约束 |
| `routers/chat_router.py` | 无重大改动 |

Tool 定义细节：

```python
create_order = {
    "type": "function",
    "function": {
        "name": "create_order",
        "description": "为用户创建订单（多轮对话确认商品、数量、地址后调用）",
        "parameters": {
            "type": "object",
            "properties": {
                "items": {
                    "type": "array",
                    "items": {
                        "type": "object",
                        "properties": {
                            "product_id": {"type": "integer"},
                            "quantity": {"type": "integer"}
                        },
                        "required": ["product_id", "quantity"]
                    },
                    "description": "商品列表，每项包含商品 ID 和数量"
                },
                "address": {"type": "string", "description": "收货地址完整文本"}
            },
            "required": ["items", "address"]
        }
    }
}
```

##### order-trade

| 文件 | 改动内容 |
|------|----------|
| `routers/internal_router.py` | 新增 `POST /internal/orders/create` 内部端点：直接创建订单（接受 `items` 列表 + `address`，无需购物车） |
| `services/order_service.py` | 新增 `create_order_direct()` 方法：事务处理 → 锁定库存 → 扣减 → 生成订单 → 返回订单 ID |

##### Nginx

无改动（`/api/ai/*` 已路由到 ai-service）

#### 2.1.5 前端改动

| 文件 | 改动内容 |
|------|----------|
| `ChatPopup.tsx` | 增强 AI 回复中的订单卡片渲染：识别 AI 返回中的订单信息（订单号、金额）渲染为可点击卡片；点击可跳转订单详情页 |
| `ChatPopup.css` | 订单卡片样式 |

#### 2.1.6 约束规则

- AI 下单必须经过用户最终确认（LLM 需输出确认语句，用户回复"是/确认/可以"后才能调用 `create_order`）
- 下单前必须确认库存充足
- 同一对话中允许用户取消上次创建但未支付的订单
- 地址优先使用用户的默认地址，用户可文本输入新地址

---

### 2.2 💳 模拟支付

#### 2.2.1 需求描述

将现有的单一 `mock` 支付升级为完整的支付模拟系统，支持多种支付方式、支付状态机、可配置延迟和支付超时。

#### 2.2.2 功能要点

| 功能 | 说明 |
|------|------|
| 多支付方式 | 模拟微信支付、支付宝、银行卡、余额四种方式 |
| 支付状态机 | 处理中(processing) → 成功(success) / 失败(failed) |
| 可配置延迟 | 可配置模拟处理延迟（默认 2 秒），在前端展示支付处理动画 |
| 支付超时 | 创建支付后若超过 configurable 时间未完成则标记为失败 |
| 支付记录增强 | 记录支付方式、支付流水号、处理时间等详情 |
| 支付失败重试 | 允许失败的支付重新发起 |

#### 2.2.3 数据库改动

**`shop.payment_records` 表增强：**

```sql
ALTER TABLE shop.payment_records ADD COLUMN IF NOT EXISTS transaction_no VARCHAR(100);       -- 流水号
ALTER TABLE shop.payment_records ADD COLUMN IF NOT EXISTS finished_at TIMESTAMPTZ;           -- 完成时间
ALTER TABLE shop.payment_records ADD COLUMN IF NOT EXISTS error_message TEXT;                -- 失败原因
-- 新增支付方式枚举约束（可选）：wechat / alipay / card / balance
-- 新增状态枚举约束：processing / success / failed
```

#### 2.2.4 后端改动

##### order-trade

| 文件 | 改动内容 |
|------|----------|
| `routers/order_router.py` | 修改 `POST /orders/{id}/pay`：接受 `payment_method` 参数；支付改为异步流程（先创建 processing 支付记录 → 后台模拟处理 → 更新状态） |
| `services/order_service.py` | 重构 `pay_order()`：拆分 `initiate_payment()` + `complete_payment()`；`initiate_payment` 加锁校验 → 创建 payment_record(status=processing) + transaction_no → 返回；`complete_payment` 加锁 → 更新状态为 success/failed → 更新 order.status |
| `services/scheduler_jobs.py` | 新增 `process_payments()` 定时任务：扫描 status=processing 的记录，模拟处理（随机成功/失败），更新结果 |
| `config.py` | 新增支付配置：`PAYMENT_DELAY_MIN=1`、`PAYMENT_DELAY_MAX=3`（秒）、`PAYMENT_TIMEOUT_MINUTES=5`、`PAYMENT_FAILURE_RATE=0.05`（5% 模拟失败率） |

#### 2.2.5 前端改动

| 文件 | 改动内容 |
|------|----------|
| `pages/order/OrderDetailPage.tsx` | 支付按钮改为弹窗选择支付方式（微信/支付宝/银行卡/余额）；支付中展示处理动画；失败后展示重试按钮 |
| `api/order.ts` | `payOrder()` 新增 `payment_method` 参数 |
| `types/index.ts` | 新增 `PaymentMethod`、`PaymentRecord` 类型 |

#### 2.2.6 AI 集成

##### ai-service

| 文件 | 改动内容 |
|------|----------|
| `services/tools.py` | 新增 Tool：`pay_order`（参数：order_id, payment_method） |
| `services/shop_client.py` | 新增 `pay_order()` 方法，调用 order-trade 支付端点 |

#### 2.2.7 前端支付交互流程

```
用户点击"去支付"
  → 弹出支付方式选择弹窗（微信 / 支付宝 / 银行卡 / 余额）
  → 用户选择支付方式 → 调用支付接口
  → 展示"支付处理中..."动画（轮询或 SSE 等待结果）
  → 支付成功：✓ 展示成功动画 → 跳转订单详情
  → 支付失败：✗ 展示失败原因 → 提供"重新支付"按钮
```

---

### 2.3 📚 RAG 客服知识库

#### 2.3.1 需求描述

基于 pgvector 向量数据库构建 FAQ 知识库，使 AI 客服能精准回答平台政策及商品知识类问题。同时将对话记录从内存存储迁移到数据库持久化。

#### 2.3.2 功能要点

| 功能 | 说明 |
|------|------|
| FAQ 向量检索 | 用户提问 → 生成 embedding → 向量相似度检索 → 匹配最佳答案 |
| 知识库覆盖范围 | 平台政策（退货/物流/支付/账户）+ 商品知识（材质/尺寸/使用说明） |
| 对话持久化 | 将对话记录存储到 `customer_service.conversations` 和 `messages` 表 |
| 知识库管理 | 提供脚本或管理接口用于添加/更新 FAQ 条目 |
| 知识库与工具协同 | 先检索 FAQ，匹配度高直接返回；匹配度低则降级为 LLM 工具调用 |

#### 2.3.3 数据库现状

**✅ 已就绪：**
- `customer_service.faq_embeddings` 表（id, question, answer, embedding vector(1024), metadata JSONB）
- pgvector 扩展 + HNSW 索引（`vector_cosine_ops`）
- `customer_service.conversations` 表 + `customer_service.messages` 表

**❌ 待实现：**
- FAQ 种子数据
- 向量检索逻辑
- 对话持久化逻辑

#### 2.3.4 后端改动

##### ai-service

| 文件 | 改动内容 |
|------|----------|
| `services/faq_service.py` | 完整实现：`generate_embedding(text)` 调用 LLM API 生成向量；`search_faq(query, threshold=0.85)` 执行 pgvector 相似度查询返回最佳答案；`add_faq(question, answer, metadata)` 添加新条目 |
| `services/conversation_service.py` | 重构为数据库持久化：`create_conversation()` → INSERT 到 `conversations` 表；`add_message()` → INSERT 到 `messages` 表；`get_history()` → SELECT 查询；需要数据库连接（新增 `database.py` 或复用 `shop_client` 的 DB 连接） |
| `routers/chat_router.py` | 在 LLM 调用前增加 FAQ 检索步骤：`_resolve_tool_calls()` 之前检查 FAQ 匹配，若匹配直接返回答案 |
| `services/llm_service.py` | 更新 `SYSTEM_PROMPT`：告知 AI 存在 FAQ 知识库可回答平台政策问题 |
| (新文件) `services/database.py` | 共享数据库连接池（ai-service 也需要访问 PostgreSQL） |
| `requirements.txt` / `Dockerfile` | 新增 `psycopg2-binary` / `asyncpg` 依赖 |
| `docker-compose.yml` | ai-service 添加 `DATABASE_URL` 环境变量 |

#### 2.3.5 FAQ 种子数据

需要预置约 20-30 条 FAQ，覆盖以下类别：

| 类别 | 示例问答 |
|------|----------|
| 退货政策 | "退货需要什么条件？" → "商品不影响二次销售，签收后 7 天内可申请退货退款" |
| 物流时效 | "下单后多久发货？" → "通常在支付成功后 24 小时内发货" |
| 支付方式 | "支持哪些支付方式？" → "支持微信支付、支付宝、银行卡、余额支付" |
| 账户安全 | "如何修改密码？" → "在个人中心 → 安全设置中可修改登录密码" |
| 商品知识 | "商品材质如何查看？" → "在商品详情页的描述中可以查看材质信息" |
| 售后 | "如何申请售后？" → "在订单详情页点击「申请售后」即可提交请求" |

#### 2.3.6 AI 问答流程（更新后）

```
用户提问
  → 生成 embedding
  → FAQ 向量检索
    ├─ 匹配度 ≥ 0.85 → 直接返回 FAQ 答案
    └─ 匹配度 < 0.85 → 进入 LLM 工具调用流程
                        → 必要时调用工具查询实时业务数据
                        → LLM 生成回答
  → 保存对话到数据库
  → 返回结果
```

---

### 2.4 ⭐ 商品评价

#### 2.4.1 需求描述

用户可以对自己购买的商品进行评价（评分 + 文字内容），支持查看商品评价列表和统计信息。AI 能够分析总结某款商品的评价，帮助用户快速了解商品口碑。

#### 2.4.2 功能要点

| 功能 | 说明 |
|------|------|
| 评价创建 | 已支付的订单中，用户可对每个商品提交评分（1-5 星）+ 文字评价 |
| 评价列表 | 商品详情页展示用户评价列表（分页），含评分、内容、用户昵称、时间 |
| 评价统计 | 展示商品平均评分、评分分布（各星级数量）、评价总数 |
| 评价权限 | 仅已购买（已支付）的用户可评价，一个商品一个订单只能评价一次 |
| AI 总结分析 | AI 客服可分析某款商品的评价，总结优缺点和用户反馈 |

#### 2.4.3 数据库改动

**新增 `shop.reviews` 表：**

```sql
CREATE TABLE IF NOT EXISTS shop.reviews (
    id          SERIAL PRIMARY KEY,
    product_id  INTEGER NOT NULL REFERENCES shop.products(id),
    user_id     INTEGER NOT NULL REFERENCES shop.users(id),
    order_id    INTEGER NOT NULL REFERENCES shop.orders(id),
    rating      SMALLINT NOT NULL CHECK(rating >= 1 AND rating <= 5),
    content     TEXT DEFAULT '',
    status      VARCHAR(20) DEFAULT 'visible',     -- visible / hidden
    created_at  TIMESTAMPTZ DEFAULT NOW(),
    updated_at  TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id, order_id, product_id)
);

CREATE INDEX idx_reviews_product ON shop.reviews(product_id, created_at DESC);
CREATE INDEX idx_reviews_user ON shop.reviews(user_id);
```

**`shop.products` 表新增缓存字段（可选加速查询）：**

```sql
ALTER TABLE shop.products ADD COLUMN IF NOT EXISTS avg_rating DECIMAL(3,2) DEFAULT 0;
ALTER TABLE shop.products ADD COLUMN IF NOT EXISTS review_count INTEGER DEFAULT 0;
```

（这些字段可通过触发器或应用层维护，也可在查询时实时计算）

#### 2.4.4 后端改动

##### service-user-product

| 文件 | 改动内容 |
|------|----------|
| (新文件) `routers/review_router.py` | `POST /c-endpoint/reviews` — 创建评价（校验订单归属、支付状态）；`GET /c-endpoint/reviews/product/{product_id}` — 商品评价列表（分页）；`GET /c-endpoint/reviews/product/{product_id}/stats` — 评价统计；`GET /c-endpoint/reviews/user/me` — 用户自己的评价 |
| (新文件) `services/review_service.py` | `create_review()` — 校验 → INSERT；`get_product_reviews()` — 分页查询；`get_review_stats()` — AVG(rating) + 各星级 COUNT；`get_user_reviews()` — 用户评价列表 |
| `main.py` | 注册 `review_router` |
| `routers/internal_router.py` | 新增 `GET /internal/reviews/product/{product_id}`、`GET /internal/reviews/product/{product_id}/stats`，供 ai-service 调用 |
| `services/product_service.py` | `get_product_by_id()` 返回结果附加 `avg_rating` 和 `review_count` |

##### Nginx

```nginx
# 新增路由
location ~ ^/api/shop/c-endpoint/reviews/(.*)$ {
    internal_rewrite /c-endpoint/reviews/$1;
    proxy_pass http://user-product:8001;
}
```

#### 2.4.5 前端改动

| 文件 | 改动内容 |
|------|----------|
| (新文件) `api/review.ts` | `createReview()`, `getProductReviews()`, `getProductReviewStats()`, `getUserReviews()` |
| `types/index.ts` | 新增 `Review`, `ReviewStats` 类型 |
| `pages/product/ProductDetailPage.tsx` | 商品信息下方新增「商品评价」Tab/区域：评价列表（评分星标 + 内容 + 用户 + 时间 + 分页）；评价统计概览（平均评分 + 评分分布条形图） |
| (新文件) `components/ReviewForm.tsx` | 评价表单：星级选择器 + 文本框 + 提交按钮 |
| `pages/order/OrderDetailPage.tsx` | 已支付且已签收的订单中，每个商品旁显示「去评价」按钮 → 弹窗填写评价 |
| (新文件) `components/StarRating.tsx` | 星级评分组件（可复用：展示用 + 交互用） |
| `pages/product/ProductDetailPage.css` | 评价区域样式 |

#### 2.4.6 AI 集成

##### ai-service

| 文件 | 改动内容 |
|------|----------|
| `services/tools.py` | 新增 Tool：`get_product_reviews`（参数：product_id）— 获取商品评价和统计 |
| `services/shop_client.py` | 新增 `get_product_reviews()` 和 `get_product_review_stats()`，调用 user-product 内部端点 |

Prompt 增强：AI 在用户询问商品口碑时，自动调用 `get_product_reviews` 获取评价数据，总结分析优缺点。

#### 2.4.7 AI 总结示例

```
用户: "智能手表 Pro 这款怎么样？"
AI 调用 get_product_reviews(product_id=1)
获取到：125 条评价，平均评分 4.2/5

AI 回复:
"📊 智能手表 Pro 综合评价分析

⭐ 平均评分：4.2 / 5（共 125 条评价）

👍 优点（用户高频好评）：
- 续航能力强，正常使用 5-7 天（32 人提及）
- 屏幕显示清晰，阳光下可见（28 人提及）
- 运动监测准确（25 人提及）

👎 不足（用户反馈）：
- 表带材质偏硬，佩戴初期不适（15 人提及）
- 消息推送偶尔有延迟（8 人提及）

总体来看是一款性价比较高的智能手表，续航和屏幕是主要卖点。需要了解具体某方面吗？"
```

---

## 3. 文件改动清单汇总

### 3.1 后端文件

| 服务 | 文件 | 操作 | 对应功能 |
|------|------|------|----------|
| **ai-service** | `services/tools.py` | 修改 | 对话下单 + 支付 + 评价分析 |
| | `services/shop_client.py` | 修改 | 新增 `create_order`, `pay_order`, `get_product_reviews` 等方法 |
| | `services/llm_service.py` | 修改 | 更新 SYSTEM_PROMPT |
| | `services/faq_service.py` | 重写 | 完整 FAQ 向量检索实现 |
| | `services/conversation_service.py` | 重写 | 数据库持久化 |
| | `services/database.py` | 新建 | 数据库连接池 |
| | `routers/chat_router.py` | 修改 | 集成 FAQ 检索步骤 |
| | `requirements.txt` | 修改 | 新增 psycopg2-binary 等依赖 |
| | `Dockerfile` 或 `docker-compose.yml` | 修改 | 新增环境变量 |
| **order-trade** | `routers/order_router.py` | 修改 | 支付接口增强 |
| | `routers/internal_router.py` | 修改 | `POST /internal/orders/create` |
| | `services/order_service.py` | 修改 | 拆分支付流程 + `create_order_direct()` |
| | `services/scheduler_jobs.py` | 修改 | 新增 `process_payments()` 任务 |
| **user-product** | `routers/review_router.py` | 新建 | 评价 CRUD 端点 |
| | `services/review_service.py` | 新建 | 评价业务逻辑 |
| | `routers/internal_router.py` | 修改 | 新增评价内部端点 |
| | `services/product_service.py` | 修改 | 商品详情附加评分信息 |
| | `main.py` | 修改 | 注册 review_router |
| **shared** | 无改动 | — | — |
| **Nginx** | `docker/nginx/nginx.conf` | 修改 | 新增 reviews 路由 |
| **DB** | `docker/init.sql` | 修改 | 新增 reviews 表 + products 扩展字段 |

### 3.2 前端文件

| 文件 | 操作 | 对应功能 |
|------|------|----------|
| `api/review.ts` | 新建 | 评价 |
| `types/index.ts` | 修改 | 评价类型 |
| `pages/product/ProductDetailPage.tsx` | 修改 | 评价列表 + 统计 |
| `components/ReviewForm.tsx` | 新建 | 评价表单 |
| `components/StarRating.tsx` | 新建 | 星级组件 |
| `pages/order/OrderDetailPage.tsx` | 修改 | 去评价入口 |
| `ChatPopup.tsx` | 修改 | 支付卡片、下单卡片、AI 评价总结展示 |
| `api/order.ts` | 修改 | payment_method 参数 |

---

## 4. 交互流程描述

### 4.1 AI 对话下单 + 支付完整流程

```
用户: "帮我买2个智能手表Pro"
AI:   (搜索商品 → find: 智能手表 Pro ¥1299)
      → "找到智能手表 Pro ¥1299/个，你的地址是...，确认下单吗？"
用户: "确认"
AI:   (调用 create_order → 订单创建成功)
      → "订单 #1024 已创建，共 ¥2598。现在支付吗？"
用户: "好的"
AI:   (调用 pay_order → 弹出支付方式选择)
      → "请选择支付方式..."
```

### 4.2 FAQ + 工具混合问答流程

```
用户: "退货政策是什么？"
AI:   (FAQ 检索命中 → 直接返回平台退货政策)
用户: "我的订单 #1024 到哪了？"
AI:   (FAQ 未命中 → 调用 get_logistics 工具 → 返回物流信息)
用户: "智能手表 Pro 用户评价怎么样？"
AI:   (FAQ 未命中 → 调用 get_product_reviews 工具 → 总结分析)
```

### 4.3 商品评价流程

```
用户下单 → 支付 → 物流配送 → 签收
  → 在订单详情页点击「去评价」
  → 选择星级（1-5）+ 填写评价内容
  → 提交 → 评价显示在商品详情页
  → AI 可调用评价接口总结分析
```

---

## 5. 数据模型变更

### 5.1 新增表

```sql
-- shop.reviews
CREATE TABLE shop.reviews (
    id          SERIAL PRIMARY KEY,
    product_id  INTEGER NOT NULL REFERENCES shop.products(id),
    user_id     INTEGER NOT NULL REFERENCES shop.users(id),
    order_id    INTEGER NOT NULL REFERENCES shop.orders(id),
    rating      SMALLINT NOT NULL CHECK(rating >= 1 AND rating <= 5),
    content     TEXT DEFAULT '',
    status      VARCHAR(20) DEFAULT 'visible',
    created_at  TIMESTAMPTZ DEFAULT NOW(),
    updated_at  TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id, order_id, product_id)
);
```

### 5.2 修改表

**`shop.products`：**
- 新增 `avg_rating DECIMAL(3,2) DEFAULT 0`
- 新增 `review_count INTEGER DEFAULT 0`

**`shop.payment_records`：**
- 新增 `transaction_no VARCHAR(100)` — 支付流水号
- 新增 `finished_at TIMESTAMPTZ` — 支付完成时间
- 新增 `error_message TEXT` — 失败原因
- `method` 枚举扩展：`wechat`, `alipay`, `card`, `balance`, `mock`
- `status` 枚举扩展：`processing`, `success`, `failed`

---

## 6. AI 工具定义汇总（更新后）

ai-service 最终将拥有以下 9 个工具：

| # | 工具名 | 用途 | 新增 |
|:-:|--------|------|:----:|
| 1 | `get_user_info` | 查用户信息 | |
| 2 | `get_orders` | 查订单列表 | |
| 3 | `get_order_detail` | 查订单详情 | |
| 4 | `get_logistics` | 查物流信息 | |
| 5 | `get_after_sales` | 查售后记录 | |
| 6 | `search_products` | 搜索商品 | |
| 7 | **`create_order`** | **创建订单（对话下单）** | ✅ |
| 8 | **`pay_order`** | **执行支付** | ✅ |
| 9 | **`get_product_reviews`** | **获取商品评价与统计** | ✅ |

---

## 7. 约束与注意事项

1. **对话下单安全**：AI 必须经过用户最终确认才能调用 `create_order`；下单前需验证库存
2. **支付原子性**：支付状态变更必须使用行级锁（`SELECT ... FOR UPDATE`）
3. **FAQ 降级策略**：FAQ 检索匹配度 < 0.85 时优雅降级到 LLM 生成，不强求匹配
4. **评价唯一约束**：一个用户对一个商品的一个订单只能评价一次（`UNIQUE(user_id, order_id, product_id)`）
5. **对话持久化**：迁移到数据库后需注意历史兼容，旧的内存对话在服务重启后丢失是预期的
6. **ai-service 新增 DB 依赖**：ai-service 目前不直接连数据库，需评估 `psycopg2` 依赖引入和连接池管理
7. **前端优先展示**：评价模块前端和后端同时开发，确保端到端可用

---

## 8. 验证方案

### 8.1 测试方式

| 模块 | 验证方法 |
|------|----------|
| AI 对话下单 | 通过 ChatPopup 输入"帮我下单" → 检查订单是否在数据库创建 |
| 模拟支付 | 选择各支付方式 → 检查支付状态流转（processing → success/failed） |
| RAG 知识库 | 输入平台政策问题 → 检查是否返回 FAQ 预置答案 |
| 商品评价 | 下单 → 支付 → 评价 → 检查评价列表和统计显示正确 |
| AI 评价总结 | 询问"xxx商品怎么样" → 检查 AI 是否调用了评价分析 |

### 8.2 集成测试

新增集成测试覆盖：

| 测试模块 | 项数 | 覆盖内容 |
|----------|:----:|----------|
| 对话下单 | 3 | 下单成功、库存不足、地址选择 |
| 支付 | 4 | 各支付方式、超时、失败重试 |
| FAQ 检索 | 2 | 命中 FAQ、未命中降级 |
| 评价 | 4 | 创建评价、列表、统计、权限校验 |

### 8.3 启动验证

```bash
# 1. 重建数据库（含新表）
docker compose -f docker/docker-compose.yml down -v
docker compose -f docker/docker-compose.yml up -d

# 2. 运行集成测试
python tests/integration_test.py

# 3. 手动测试对话下单
curl -X POST http://localhost/api/ai/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"帮我下单买1个智能手表Pro","user_id":1}'
```

---

## 9. 开发优先级建议

| 优先级 | 模块 | 理由 |
|:------:|------|------|
| P0 | 商品评价（后端 + 数据库） | 数据模型变更，所有后续依赖此结构 |
| P0 | 模拟支付增强 | 订单流程的核心环节，对话下单需要支付配合 |
| P1 | RAG 知识库 | 独立模块，可与 P0 并行开发 |
| P1 | AI 对话下单 | 依赖支付流程稳定后开发 |
| P2 | 商品评价（前端 + AI 集成） | 后端完成后进行 |
| P2 | AI 评价总结分析 | 评价数据积累后开发 |

---

> **本文档为第二轮开发需求规格说明，具体实现细节以技术设计文档为准。**
